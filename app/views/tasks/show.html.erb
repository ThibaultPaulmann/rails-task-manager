<div class="d-flex justify-content-between">
  <h1><%= @task.title %></h1>
  <a href="<%= edit_task_path(@task) %>"><button type="button" class="btn btn-outline-secondary">Edit</button></a>
</div>
<h3 class="alert alert-secondary"><%= @task.details %></h3>
<input type="checkbox" style="pointer-events: none;" <%= "checked" if @task.completed %>> <label> Completed </label><br><br>

<%= link_to "Back to list", tasks_path %>